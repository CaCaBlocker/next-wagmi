{
  "private": true,
  "scripts": {
    "dev": "next",
    "build": "next build",
    "start": "next start -p $PORT",
    "heroku-postbuild": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "ethers": "^5.6.2",
    "next": "latest",
    "react": "17.0.2",
    "react-dom": "17.0.2",
    "wagmi": "^0.2.25"
  }
}